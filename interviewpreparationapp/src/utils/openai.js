export const generateAIResponse = async (prompt) => {
  console.log("Mock AI Response for prompt:", prompt);

  // Simulating AI-generated interview questions
  return `
    1. What is React and how does it work?
    2. Explain the difference between state and props in React.
    3. What are React Hooks? Can you name a few?
    4. How does React handle performance optimization?
    5. What is the Virtual DOM, and why is it useful?
  `;
};
